import React from "react";

import HotelRoundedIcon from "@mui/icons-material/HotelRounded";

export default function AccomodationIcon({ color }: { color: string }) {
  return <HotelRoundedIcon sx={{ color }} />;
}
