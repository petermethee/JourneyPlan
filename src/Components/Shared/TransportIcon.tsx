import React from "react";
import TrainRoundedIcon from "@mui/icons-material/TrainRounded";

export default function TransportIcon({ color }: { color: string }) {
  return <TrainRoundedIcon sx={{ color }} />;
}
